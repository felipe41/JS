/* console.log('Linha 0');
console.log('Linha 1');
console.log('Linha 2');
console.log('Linha 3');
console.log('Linha 4');
console.log('Linha 5'); */

// i - significa index
/*

for(let i = 500; i >= 400; i -=10) {
    console.log('Linha ${i}');      // Para decrementar. Caso queira incrementar, é só mudar o sinal de menos
}

for(let i = 0; i <= 10; i++) {
    const par = i % 2 === 0;  // Verifica o resto da divisão e mostra se é par ou ímpar
    console.log(i, par);
}
                 OU 

for(let i = 0; i <= 10; i++) {
    const par = i % 2 === 0 ?  'par' : 'ímpar'   // Mostra o nome da variável na tela 
    console.log(i, par);
}

const frutas = ['Maça', 'Pera', 'Uva'];

for(let i = 0; i < frutas.length; i++) {
    console.log(i);  // Length mostra a quantidade de elementos dentro do array
}

for(let i = 0; i < frutas.length; i++) {
    console.log(`Índice ${i}`, frutas[i]); // Põe nome em cada elemento chamando de índice
} 
*/
